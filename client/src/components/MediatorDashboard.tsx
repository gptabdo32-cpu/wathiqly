import React, { useState } from "react";\nimport { Card } from \"@/components/ui/card\";\nimport { Button } from \"@/components/ui/button\";\nimport { Badge } from \"@/components/ui/badge\";\nimport { Dialog, DialogContent, DialogHeader, DialogTitle } from \"@/components/ui/dialog\";\nimport { trpc } from \"@/lib/trpc\";\nimport { useAuth } from \"@/core/hooks/useAuth\";\nimport { toast } from \"sonner\";\nimport {\n  Clock,\n  CheckCircle,\n  AlertCircle,\n  MessageCircle,\n  User,\n  DollarSign,\n  Calendar,\n} from \"lucide-react\";\n\ninterface MediatorRequest {\n  id: number;\n  conversationId: number;\n  escrowId: number;\n  requestedBy: number;\n  reason: string;\n  fee: string;\n  status: \"pending\" | \"accepted\" | \"active\" | \"resolved\" | \"cancelled\";\n  requestedAt: Date;\n  acceptedAt?: Date;\n}\n\nexport default function MediatorDashboard() {\n  const { user } = useAuth();\n  const [selectedRequest, setSelectedRequest] = useState<MediatorRequest | null>(null);\n  const [isDialogOpen, setIsDialogOpen] = useState(false);\n\n  const { data: pendingRequests, isLoading } = trpc.mediatorAdmin.getPendingRequests.useQuery();\n\n  const handleAcceptRequest = async (requestId: number) => {\n    try {\n      // This would call the accept mediator request endpoint\n      toast.success(\"تم قبول الطلب بنجاح\");\n      setIsDialogOpen(false);\n    } catch (error) {\n      toast.error(\"فشل قبول الطلب\");\n    }\n  };\n\n  const getStatusBadge = (status: string) => {\n    switch (status) {\n      case \"pending\":\n        return (\n          <Badge className=\"bg-yellow-100 text-yellow-800 flex items-center gap-1\">\n            <Clock className=\"w-3 h-3\" />\n            قيد الانتظار\n          </Badge>\n        );\n      case \"accepted\":\n        return (\n          <Badge className=\"bg-blue-100 text-blue-800 flex items-center gap-1\">\n            <AlertCircle className=\"w-3 h-3\" />\n            مقبول\n          </Badge>\n        );\n      case \"active\":\n        return (\n          <Badge className=\"bg-green-100 text-green-800 flex items-center gap-1\">\n            <CheckCircle className=\"w-3 h-3\" />\n            نشط\n          </Badge>\n        );\n      case \"resolved\":\n        return (\n          <Badge className=\"bg-purple-100 text-purple-800 flex items-center gap-1\">\n            <CheckCircle className=\"w-3 h-3\" />\n            تم الحل\n          </Badge>\n        );\n      default:\n        return <Badge>غير معروف</Badge>;\n    }\n  };\n\n  return (\n    <div className=\"space-y-6\">\n      {/* Header */}\n      <div className=\"bg-gradient-to-r from-blue-600 to-blue-700 text-white p-6 rounded-lg\">\n        <h1 className=\"text-3xl font-bold mb-2\">لوحة تحكم الوسيط</h1>\n        <p className=\"text-blue-100\">\n          إدارة طلبات الوسيط والنزاعات\n        </p>\n      </div>\n\n      {/* Stats */}\n      <div className=\"grid grid-cols-1 md:grid-cols-3 gap-4\">\n        <Card className=\"p-4 border-l-4 border-yellow-500\">\n          <div className=\"flex items-center justify-between\">\n            <div>\n              <p className=\"text-gray-600 text-sm\">طلبات معلقة</p>\n              <p className=\"text-2xl font-bold text-yellow-600\">\n                {pendingRequests?.filter((r) => r.status === \"pending\").length || 0}\n              </p>\n            </div>\n            <Clock className=\"w-8 h-8 text-yellow-500\" />\n          </div>\n        </Card>\n\n        <Card className=\"p-4 border-l-4 border-blue-500\">\n          <div className=\"flex items-center justify-between\">\n            <div>\n              <p className=\"text-gray-600 text-sm\">نشطة حالياً</p>\n              <p className=\"text-2xl font-bold text-blue-600\">\n                {pendingRequests?.filter((r) => r.status === \"active\").length || 0}\n              </p>\n            </div>\n            <MessageCircle className=\"w-8 h-8 text-blue-500\" />\n          </div>\n        </Card>\n\n        <Card className=\"p-4 border-l-4 border-green-500\">\n          <div className=\"flex items-center justify-between\">\n            <div>\n              <p className=\"text-gray-600 text-sm\">تم حلها</p>\n              <p className=\"text-2xl font-bold text-green-600\">\n                {pendingRequests?.filter((r) => r.status === \"resolved\").length || 0}\n              </p>\n            </div>\n            <CheckCircle className=\"w-8 h-8 text-green-500\" />\n          </div>\n        </Card>\n      </div>\n\n      {/* Requests List */}\n      <Card>\n        <div className=\"p-6\">\n          <h2 className=\"text-xl font-bold mb-4\">الطلبات</h2>\n\n          {isLoading ? (\n            <div className=\"text-center py-8 text-gray-500\">\n              جاري التحميل...\n            </div>\n          ) : !pendingRequests || pendingRequests.length === 0 ? (\n            <div className=\"text-center py-8 text-gray-500\">\n              لا توجد طلبات معلقة\n            </div>\n          ) : (\n            <div className=\"space-y-3\">\n              {pendingRequests.map((request) => (\n                <div\n                  key={request.id}\n                  className=\"border rounded-lg p-4 hover:bg-gray-50 transition-colors\"\n                >\n                  <div className=\"flex items-start justify-between\">\n                    <div className=\"flex-1\">\n                      <div className=\"flex items-center gap-3 mb-2\">\n                        <div className=\"w-10 h-10 bg-blue-100 rounded-full flex items-center justify-center\">\n                          <User className=\"w-5 h-5 text-blue-600\" />\n                        </div>\n                        <div>\n                          <p className=\"font-semibold\">طلب رقم #{request.id}</p>\n                          <p className=\"text-sm text-gray-600\">\n                            <Calendar className=\"w-3 h-3 inline mr-1\" />\n                            {new Date(request.requestedAt).toLocaleString(\"ar-LY\")}\n                          </p>\n                        </div>\n                      </div>\n\n                      <p className=\"text-sm text-gray-700 mb-2\">\n                        <span className=\"font-semibold\">السبب:</span> {request.reason}\n                      </p>\n\n                      <div className=\"flex items-center gap-4 text-sm\">\n                        <span className=\"flex items-center gap-1 text-gray-600\">\n                          <DollarSign className=\"w-4 h-4\" />\n                          رسم: {request.fee} دينار\n                        </span>\n                        <span className=\"flex items-center gap-1 text-gray-600\">\n                          <MessageCircle className=\"w-4 h-4\" />\n                          دردشة #{request.conversationId}\n                        </span>\n                      </div>\n                    </div>\n\n                    <div className=\"flex flex-col items-end gap-2\">\n                      {getStatusBadge(request.status)}\n\n                      {request.status === \"pending\" && (\n                        <Button\n                          onClick={() => {\n                            setSelectedRequest(request);\n                            setIsDialogOpen(true);\n                          }}\n                          className=\"bg-green-600 hover:bg-green-700 text-white text-xs\"\n                        >\n                          قبول الطلب\n                        </Button>\n                      )}\n\n                      <Button\n                        variant=\"outline\"\n                        className=\"text-xs\"\n                        onClick={() => {\n                          setSelectedRequest(request);\n                          setIsDialogOpen(true);\n                        }}\n                      >\n                        التفاصيل\n                      </Button>\n                    </div>\n                  </div>\n                </div>\n              ))}\n            </div>\n          )}\n        </div>\n      </Card>\n\n      {/* Details Dialog */}\n      <Dialog open={isDialogOpen} onOpenChange={setIsDialogOpen}>\n        <DialogContent className=\"max-w-md\">\n          <DialogHeader>\n            <DialogTitle>تفاصيل الطلب #{selectedRequest?.id}</DialogTitle>\n          </DialogHeader>\n\n          {selectedRequest && (\n            <div className=\"space-y-4\">\n              <div>\n                <p className=\"text-sm text-gray-600\">السبب</p>\n                <p className=\"font-semibold\">{selectedRequest.reason}</p>\n              </div>\n\n              <div className=\"grid grid-cols-2 gap-4\">\n                <div>\n                  <p className=\"text-sm text-gray-600\">الرسم</p>\n                  <p className=\"font-semibold\">{selectedRequest.fee} دينار</p>\n                </div>\n                <div>\n                  <p className=\"text-sm text-gray-600\">الحالة</p>\n                  <p className=\"font-semibold\">{getStatusBadge(selectedRequest.status)}</p>\n                </div>\n              </div>\n\n              <div>\n                <p className=\"text-sm text-gray-600\">وقت الطلب</p>\n                <p className=\"font-semibold\">\n                  {new Date(selectedRequest.requestedAt).toLocaleString(\"ar-LY\")}\n                </p>\n              </div>\n\n              {selectedRequest.status === \"pending\" && (\n                <Button\n                  onClick={() => handleAcceptRequest(selectedRequest.id)}\n                  className=\"w-full bg-green-600 hover:bg-green-700 text-white\"\n                >\n                  قبول الطلب\n                </Button>\n              )}\n            </div>\n          )}\n        </DialogContent>\n      </Dialog>\n    </div>\n  );\n}\n
